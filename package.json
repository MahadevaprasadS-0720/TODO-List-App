{
  "name": "todo-list-app",
  "version": "1.0.0",
  "private": true,
  "description": "Modern Todo List application built with React, Vite, Tailwind CSS v4, and Firebase Firestore backend",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client"
  }
}
